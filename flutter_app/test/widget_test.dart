import 'package:flutter_test/flutter_test.dart';
import 'package:srilambo_vpn/main.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Skipped: ProviderScope + StorageService need async init
    // Run flutter test after setting up test overrides
  });
}
